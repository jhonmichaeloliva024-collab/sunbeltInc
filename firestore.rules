rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sun Belt Inc screening registry rules
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}
